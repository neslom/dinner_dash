<h3><%= current_user.username %>'s Orders</h3>

<ol>
  <% @orders.each do |order| %>
    <% order.cart.each do |item_id, quantity| %>
    <li><%= Item.find(item_id).name %>
        <%= quantity %>
    </li>
    <% end %>
  <% end %>
</ol>

