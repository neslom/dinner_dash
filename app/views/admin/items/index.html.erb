<div id="menu">
  <h1>Manage Items</h1>
  <div class="row">
    <% @items.each do |item| %>
      <div class="floaters">
        <h4 class="item-title"> <%= link_to  item.name, item %> </hr>
      </div>
    <% end %>
  </div>
</div>
